# Suhail runtime artifacts (when developing against a target repo inside this checkout)
.suhail/
.suhail-*.txt

# Claude Code local runtime files (nothing under .claude/ is tracked)
.claude/

# Internal working notes — never ship
MEMORY.md
AGENTS.local.md

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
